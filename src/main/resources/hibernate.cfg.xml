<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration SYSTEM "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Basic configuration -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>

        <!-- Database configuration -->
        <property name="hibernate.hbm2ddl.auto">create</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">domiklein</property>

        <!-- Output settings -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">false</property>

        <!-- Mappings -->
        <mapping class="com.bugtracker.database.model.User"/>
        <mapping class="com.bugtracker.database.model.Ticket"/>
        <mapping class="com.bugtracker.database.model.TicketComment"/>

    </session-factory>
</hibernate-configuration>